name: CI
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        database: [sqlite, postgresql]
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: euaiact
          POSTGRES_PASSWORD: euaiact
          POSTGRES_DB: euaiact_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -e ".[dev]"
      - name: Tests
        run: pytest -q
        env:
          EUAIACT_TEST_DATABASE_URL: ${{ matrix.database == 'postgresql' && 'postgresql+psycopg://euaiact:euaiact@localhost/euaiact_test' || '' }}
      # Report (without failing) legal dates still awaiting verification.
      # Release pipelines should run with --strict so unverified dates block a release.
      - run: python -m euaiact.cli check-legal-dates
